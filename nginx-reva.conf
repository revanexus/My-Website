server {
  listen 80;
  server_name reva.example.com www.reva.example.com;
  client_max_body_size 85m;
  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
  }
}
# After issuing a certificate with Certbot, redirect HTTP to HTTPS and add:
# listen 443 ssl http2;
# ssl_certificate /etc/letsencrypt/live/reva.example.com/fullchain.pem;
# ssl_certificate_key /etc/letsencrypt/live/reva.example.com/privkey.pem;
