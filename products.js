document.addEventListener('DOMContentLoaded',async()=>{const grid=document.querySelector('#productsGrid');try{const r=await fetch('/api/products');const items=await r.json();grid.innerHTML=items.length?items.map(p=>`<article class="card"><div class="eyebrow">${esc(p.product_type)}</div><h2>${esc(p.name)}</h2><p>${esc(p.description||'')}</p><strong>${esc(String(p.price??''))} ${esc(p.currency||'USD')}</strong><br><br><a class="btn" href="product.html?slug=${encodeURIComponent(p.slug)}">View product →</a></article>`).join(''):'<p>No published products yet.</p>'}catch(e){grid.innerHTML='<p>Products are temporarily unavailable.</p>'}});function esc(x){return String(x??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
